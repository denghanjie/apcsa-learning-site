const fs = require("fs");
const path = require("path");

const root = path.resolve(__dirname, "..", "Detailed_Tutorials");
const outputFile = path.resolve(__dirname, "tutorial-data.js");

const unitFiles = [
  {
    unit: 1,
    title: "Unit 1: Using Objects and Methods",
    file: "Unit1_Detailed_Tutorials.md"
  },
  {
    unit: 2,
    title: "Unit 2: Selection and Iteration",
    file: "Unit2_Detailed_Tutorials.md"
  },
  {
    unit: 3,
    title: "Unit 3: Class Creation",
    file: "Unit3_Detailed_Tutorials.md"
  },
  {
    unit: 4,
    title: "Unit 4: Data Collections",
    file: "Unit4_Detailed_Tutorials.md"
  }
];

function bridgeLesson(id, title, bodyLines) {
  return {
    id,
    title,
    unit: "Bridge",
    type: "bridge",
    source: "Guided learning path",
    body: bodyLines.join("\n")
  };
}

function examLab(id, title, bodyLines) {
  return {
    id,
    title,
    unit: "Exam Lab",
    type: "lab",
    source: "Current AP-style exam pattern lab",
    body: bodyLines.join("\n")
  };
}

const bridgeLessons = [
  bridgeLesson("B-OBJECT-MODEL", "Bridge: From Values to Objects", [
    "### Why This Bridge Exists",
    "Early Java feels like arithmetic: store numbers, combine them, print answers. Objects add a new question: what if one value is not enough to describe the thing we care about?",
    "",
    "A `String` is not just letters. It is an object with stored text and methods such as `length`, `substring`, and `indexOf`. The same mental model will later apply to `Student`, `ScoreReport`, `ArrayList`, and arrays of objects.",
    "",
    "### Learn",
    "A primitive variable stores a direct value. A reference variable stores a way to find an object.",
    "",
    "| Kind | Example | What the variable holds |",
    "| --- | --- | --- |",
    "| primitive | `int score = 95;` | the number 95 |",
    "| reference | `String name = \"Ava\";` | a reference to a String object |",
    "",
    "This matters because method calls use the object that the reference points to.",
    "",
    "```java",
    "String word = \"study\";",
    "int count = word.length();",
    "```",
    "",
    "`word.length()` means: go to the `String` object referred to by `word`, then ask that object for its length.",
    "",
    "### Predict",
    "Before running the code, decide what prints.",
    "",
    "```java",
    "String a = \"APCSA\";",
    "String b = a.substring(2);",
    "System.out.println(a);",
    "System.out.println(b);",
    "```",
    "",
    "### Misconception Check",
    "- Calling a method does not always change the object. Many `String` methods return a new value.",
    "- A reference variable is not the whole object. It is the handle used to reach the object.",
    "- `null` means the reference currently points to no object.",
    "",
    "### Practice",
    "Explain in one sentence why `word.length()` needs parentheses but `score` does not.",
    "",
    "Possible answer: `length` is a method call that asks an object to do work and return a result; `score` is just a variable value."
  ]),
  bridgeLesson("B-CLASS-DESIGN-STORY", "Bridge: From Using Objects to Designing Objects", [
    "### Why This Bridge Exists",
    "Unit 1 teaches you to use objects that Java already gives you. Unit 3 asks you to design your own objects. The shift is from consumer to builder.",
    "",
    "A good class answers three questions: what data should each object remember, what actions should it support, and what details should stay hidden?",
    "",
    "### Learn",
    "Imagine a quiz app needs many questions. A plain set of variables becomes messy:",
    "",
    "```java",
    "String prompt1 = \"What is 2 + 3?\";",
    "int answer1 = 5;",
    "String prompt2 = \"What is 4 + 1?\";",
    "int answer2 = 5;",
    "```",
    "",
    "A class groups related data and behavior.",
    "",
    "```java",
    "public class Question {",
    "  private String prompt;",
    "  private int answer;",
    "",
    "  public Question(String p, int a) {",
    "    prompt = p;",
    "    answer = a;",
    "  }",
    "",
    "  public boolean isCorrect(int response) {",
    "    return response == answer;",
    "  }",
    "}",
    "```",
    "",
    "### Predict",
    "Which parts belong to each object separately? Which part is the method every `Question` object can perform?",
    "",
    "### Misconception Check",
    "- A class is the blueprint; an object is one actual instance created from that blueprint.",
    "- Instance variables should usually be `private` because other code should interact through methods.",
    "- A constructor initializes a new object; it does not have a return type.",
    "",
    "### Practice",
    "Design the instance variables for a `Song` class. Then write one method idea that would use those variables.",
    "",
    "Possible answer: `private String title; private int seconds;` and a method `isLong()` that returns whether `seconds > 300`."
  ]),
  bridgeLesson("B-ARRAYLIST-NEED", "Bridge: Why Arrays Are Not Always Enough", [
    "### Why This Bridge Exists",
    "Arrays are perfect when the size is known and stable. Many real problems are not like that. A diagnostic app might collect responses one at a time, and the number of answers may grow as students work.",
    "",
    "That is the problem `ArrayList` solves: a sequence whose size can change.",
    "",
    "### Learn",
    "Use an array when the length is fixed:",
    "",
    "```java",
    "int[] scores = new int[5];",
    "scores[0] = 92;",
    "```",
    "",
    "Use an `ArrayList` when items may be added or removed:",
    "",
    "```java",
    "ArrayList<String> names = new ArrayList<String>();",
    "names.add(\"Ava\");",
    "names.add(\"Leo\");",
    "names.remove(0);",
    "```",
    "",
    "The main tradeoff is that an array uses bracket access and has a fixed `length`, while an `ArrayList` uses methods such as `add`, `get`, `set`, `remove`, and `size`.",
    "",
    "### Predict",
    "What does this code print?",
    "",
    "```java",
    "ArrayList<String> names = new ArrayList<String>();",
    "names.add(\"Ava\");",
    "names.add(\"Leo\");",
    "names.add(1, \"Mia\");",
    "System.out.println(names.get(1));",
    "System.out.println(names.size());",
    "```",
    "",
    "### Misconception Check",
    "- `arr.length` is a field; `list.size()` is a method.",
    "- `list.get(i)` reads; `list.set(i, value)` replaces; `list.add(value)` grows the list.",
    "- Removing an item shifts later items left, so indexes can change during traversal.",
    "",
    "### Practice",
    "Write one sentence explaining why a classroom roster is a better fit for `ArrayList<String>` than `String[]` when students may join or leave."
  ]),
  bridgeLesson("B-ARRAYLIST-BOXING", "Bridge: Wrappers, Autoboxing, and ArrayList", [
    "### Why This Bridge Exists",
    "`ArrayList` stores objects, not primitive values. That is why `ArrayList<int>` is not valid Java. To store numbers in an `ArrayList`, Java uses wrapper classes such as `Integer` and `Double`.",
    "",
    "### Learn",
    "Use wrapper types in the type parameter:",
    "",
    "```java",
    "ArrayList<Integer> scores = new ArrayList<Integer>();",
    "scores.add(95);        // autoboxing: int becomes Integer",
    "int first = scores.get(0); // unboxing: Integer becomes int",
    "```",
    "",
    "Autoboxing is Java's automatic conversion from primitive to wrapper. Unboxing is the reverse.",
    "",
    "| Primitive | Wrapper | Example use in ArrayList |",
    "| --- | --- | --- |",
    "| `int` | `Integer` | `ArrayList<Integer>` |",
    "| `double` | `Double` | `ArrayList<Double>` |",
    "| `boolean` | `Boolean` | `ArrayList<Boolean>` |",
    "",
    "### Predict",
    "What is the value of `total`?",
    "",
    "```java",
    "ArrayList<Integer> scores = new ArrayList<Integer>();",
    "scores.add(10);",
    "scores.add(20);",
    "int total = scores.get(0) + scores.get(1);",
    "```",
    "",
    "### Misconception Check",
    "- `ArrayList<int>` is invalid because type parameters require reference types.",
    "- `Integer` objects can often be used in arithmetic because Java unboxes them.",
    "- Unboxing a `null` wrapper causes an error, because there is no primitive value inside.",
    "",
    "### Practice",
    "Declare an `ArrayList` that can store decimal quiz averages. Then add `87.5` to it.",
    "",
    "Possible answer: `ArrayList<Double> averages = new ArrayList<Double>(); averages.add(87.5);`"
  ]),
  bridgeLesson("B-COLLECTION-REFERENCES", "Bridge: Objects Inside Arrays and ArrayLists", [
    "### Why This Bridge Exists",
    "After class design, arrays and ArrayLists are no longer just collections of numbers or strings. They can store references to objects from your own classes, such as `Student`, `Question`, or `ScoreReport`.",
    "",
    "This is where many AP CSA traces become subtle: are you changing the collection, or are you changing the object that an element refers to?",
    "",
    "### Learn",
    "A class can have another object or collection as an instance variable. This is a has-a relationship.",
    "",
    "```java",
    "public class Course {",
    "  private ArrayList<Student> roster;",
    "",
    "  public Course() {",
    "    roster = new ArrayList<Student>();",
    "  }",
    "}",
    "```",
    "",
    "Read this as: a `Course` has a roster. The roster has references to `Student` objects.",
    "",
    "Arrays follow the same idea.",
    "",
    "```java",
    "Student[] roster = new Student[3];",
    "roster[0] = new Student(\"Ada\", 92);",
    "roster[0].setScore(95);",
    "```",
    "",
    "`roster[0].setScore(95)` follows the reference at index 0 and mutates the `Student` object.",
    "",
    "### Mutate vs Replace",
    "Mutating an object keeps the same element reference:",
    "",
    "```java",
    "roster.get(0).setScore(100);",
    "```",
    "",
    "Replacing an element stores a different reference in the collection:",
    "",
    "```java",
    "roster.set(0, new Student(\"Mia\", 88));",
    "```",
    "",
    "### Predict",
    "Suppose `first` and `roster.get(0)` refer to the same `Student` object. What happens after this code?",
    "",
    "```java",
    "Student first = roster.get(0);",
    "first.setScore(99);",
    "System.out.println(roster.get(0).getScore());",
    "```",
    "",
    "### Misconception Check",
    "- An array or ArrayList element can hold a reference to an object.",
    "- Calling a mutator on that reference can change the object.",
    "- Assigning an enhanced-for loop variable does not replace the element in the collection.",
    "- To replace an ArrayList element, use `set(index, newObject)`.",
    "- To replace an array element, assign `arr[index] = newObject`.",
    "",
    "### Practice",
    "Write one line that changes the score of the first `Student` in `ArrayList<Student> roster` to 90.",
    "",
    "Possible answer: `roster.get(0).setScore(90);`"
  ]),
  bridgeLesson("B-ARRAY-TO-2D", "Bridge: From One Row to a Grid", [
    "### Why This Bridge Exists",
    "A regular array models one sequence. A 2D array models rows and columns. This is useful for seats, images, game boards, spreadsheets, and tables of test data.",
    "",
    "### Learn",
    "Think of `grid[row][col]` as two choices: choose a row, then choose an item inside that row.",
    "",
    "```java",
    "int[][] grid = {",
    "  {4, 7, 1},",
    "  {9, 2, 6}",
    "};",
    "",
    "System.out.println(grid[0][1]); // 7",
    "System.out.println(grid.length); // number of rows: 2",
    "System.out.println(grid[0].length); // columns in row 0: 3",
    "```",
    "",
    "### Predict",
    "Which value is printed by `grid[1][2]` in the example above?",
    "",
    "### Misconception Check",
    "- The first index is the row, not the column.",
    "- `grid.length` is the number of rows.",
    "- `grid[r].length` is the number of columns in row `r`.",
    "",
    "### Practice",
    "Write nested loop headers that visit every cell in row-major order.",
    "",
    "Possible answer: `for (int r = 0; r < grid.length; r++)` and inside it `for (int c = 0; c < grid[r].length; c++)`."
  ]),
  bridgeLesson("B-ITERATION-RECURSION", "Bridge: From Loops to Recursion", [
    "### Why This Bridge Exists",
    "Loops repeat by moving a variable toward a stopping condition. Recursion repeats by calling the same method on a smaller version of the problem.",
    "",
    "### Learn",
    "Every AP CSA recursive method needs two ideas: a base case and a recursive call.",
    "",
    "```java",
    "public static int sumTo(int n) {",
    "  if (n == 1) {",
    "    return 1;",
    "  }",
    "  return n + sumTo(n - 1);",
    "}",
    "```",
    "",
    "The base case stops the chain. The recursive call makes progress toward the base case.",
    "",
    "### Predict",
    "Trace `sumTo(4)`. Write the pending additions before calculating the final answer.",
    "",
    "### Misconception Check",
    "- A recursive method is not magic; it creates a chain of method calls.",
    "- Forgetting the base case usually causes infinite recursion.",
    "- A recursive call must move toward the base case.",
    "",
    "### Practice",
    "For a method that counts down from `n` to 1, identify the base case and the smaller recursive call.",
    "",
    "Possible answer: base case `n == 1`; smaller call uses `n - 1`."
  ]),
  bridgeLesson("B-FRQ-READING", "Bridge: Reading FRQs Like Specifications", [
    "### Why This Bridge Exists",
    "Students often lose FRQ points because they start coding before extracting the contract. FRQs are not just coding prompts; they are specifications with required behavior, restrictions, and examples.",
    "",
    "### Learn",
    "Before writing code, mark four things:",
    "",
    "1. Inputs: parameters, instance variables, and any helper methods already provided.",
    "2. Output: return value or mutation that must happen.",
    "3. Rules: restrictions such as not modifying a list, using a helper method, or preserving order.",
    "4. Examples: sample cases that reveal edge behavior.",
    "",
    "A strong FRQ solution usually starts as a short algorithm in English.",
    "",
    "```java",
    "// Goal: count values above threshold",
    "// Plan: traverse every item, test it, update count, return count",
    "```",
    "",
    "### Predict",
    "If a prompt says a method should return a value, should you print the answer? Explain.",
    "",
    "### Misconception Check",
    "- Do not add input prompts or extra print statements unless the FRQ asks for them.",
    "- Do not rewrite provided classes or helper methods.",
    "- Keep variable names simple and traceable; clarity helps avoid small logic mistakes.",
    "",
    "### Practice",
    "For any FRQ part, write a three-line plan: data to inspect, condition to test, value to return or update."
  ])
];

const examLabLessons = [
  examLab("L-MODULO-PATTERNS", "Modulo Patterns: Remainders as Information", [
    "### Why This Lab Matters",
    "The remainder operator is rarely tested as a bare arithmetic fact. It usually appears inside a decision, a loop, or a collection algorithm.",
    "",
    "### Core Pattern",
    "Use `%` when the question is about divisibility, parity, cycles, or digits.",
    "",
    "```java",
    "n % 2 == 0      // even",
    "n % 2 != 0      // odd",
    "n % k == 0      // divisible by k",
    "n % 10          // ones digit",
    "(n / 10) % 10   // tens digit",
    "```",
    "",
    "### Exam Pattern 1: Odd or Even Size",
    "When an algorithm behaves differently for an odd number of items, test the size before choosing bounds.",
    "",
    "```java",
    "if (list.size() % 2 == 1) {",
    "  // odd case",
    "} else {",
    "  // even case",
    "}",
    "```",
    "",
    "### Exam Pattern 2: Repeating Cycles",
    "A cycle repeats when the remainder repeats. This appears in row patterns, alternating turns, and every-nth-item logic.",
    "",
    "```java",
    "if (index % 3 == 0) {",
    "  // index is 0, 3, 6, 9, ...",
    "}",
    "```",
    "",
    "### Common Traps",
    "- `a % b == 0` means `a` is divisible by `b`, not the other way around.",
    "- `n % 2 == 1` works for positive odd integers; `n % 2 != 0` is safer if negative values are possible.",
    "- `%` has the same precedence level as `*` and `/`, before `+` and `-`.",
    "",
    "### Practice",
    "Write conditions for these cases:",
    "",
    "1. `num` is a multiple of 5.",
    "2. `num` has ones digit 7.",
    "3. `items` has an odd number of elements.",
    "4. `row` should be treated as a special row every fourth row.",
    "",
    "<details class=\"answer-toggle\"><summary>Show worked answer</summary><div class=\"answer-body\">",
    "",
    "```java",
    "num % 5 == 0",
    "num % 10 == 7",
    "items.size() % 2 == 1",
    "row % 4 == 0",
    "```",
    "",
    "</div></details>"
  ]),
  examLab("L-STRING-SURGERY", "String Surgery with indexOf and substring", [
    "### Why This Lab Matters",
    "Recent AP-style string tasks often ask whether a piece of text is absent, at the beginning, at the end, or somewhere in the middle. The code is small, but the index arithmetic is easy to damage.",
    "",
    "### Core Pattern",
    "`indexOf` answers location. `substring` builds the new string.",
    "",
    "```java",
    "int pos = text.indexOf(target);",
    "if (pos == -1) {",
    "  // target is absent",
    "}",
    "```",
    "",
    "### Contains, Prefix, and Suffix",
    "Use the comparison that matches the exact location in the specification.",
    "",
    "```java",
    "text.indexOf(target) != -1          // appears anywhere",
    "text.indexOf(target) == 0           // starts with target",
    "text.substring(text.length() - target.length()).equals(target)  // ends with target",
    "```",
    "",
    "Guard the suffix check when `text` might be shorter than `target`.",
    "",
    "```java",
    "if (text.length() >= target.length()) {",
    "  String end = text.substring(text.length() - target.length());",
    "  if (end.equals(target)) {",
    "    // suffix match",
    "  }",
    "}",
    "```",
    "",
    "### Cut and Replace",
    "To remove the first occurrence, keep the part before it and the part after it.",
    "",
    "```java",
    "int pos = text.indexOf(target);",
    "if (pos != -1) {",
    "  text = text.substring(0, pos) + text.substring(pos + target.length());",
    "}",
    "```",
    "",
    "To replace it, put the new text in the middle.",
    "",
    "```java",
    "text = text.substring(0, pos) + replacement + text.substring(pos + target.length());",
    "```",
    "",
    "### Common Traps",
    "- `indexOf(target) > 0` misses a prefix match at index 0.",
    "- `substring(start, end)` includes `start` but excludes `end`.",
    "- `substring(target.length())` removes a prefix only after you have confirmed the prefix exists.",
    "- Use `.equals`, not `==`, when comparing string contents.",
    "",
    "### Practice",
    "Write code that returns `word` with the prefix `pre` removed if `word` starts with `pre`; otherwise return `word` unchanged.",
    "",
    "<details class=\"answer-toggle\"><summary>Show worked answer</summary><div class=\"answer-body\">",
    "",
    "```java",
    "if (word.indexOf(pre) == 0) {",
    "  return word.substring(pre.length());",
    "}",
    "return word;",
    "```",
    "",
    "</div></details>"
  ]),
  examLab("L-METHOD-SIDE-EFFECTS", "Method Calls, Return Values, and Side Effects", [
    "### Why This Lab Matters",
    "Current FRQs often give you helper methods. Students lose points when they call the helper on the wrong object, omit required parameters, treat a void method like it returns a value, or call a state-changing method more than once.",
    "",
    "### Core Pattern",
    "Read a helper method by asking three questions:",
    "",
    "1. What object receives the call?",
    "2. What parameters are required?",
    "3. Does it return a value, or does it mutate state?",
    "",
    "### Same Object vs Another Object",
    "A method in the same class can usually be called directly.",
    "",
    "```java",
    "int walked = walkDogs(hour);",
    "```",
    "",
    "A method belonging to an instance variable must be called through that reference.",
    "",
    "```java",
    "int available = company.numAvailableDogs(hour);",
    "company.updateDogs(hour, walked);",
    "```",
    "",
    "### Void Method Pattern",
    "A void method performs an action. Do not return it or store it.",
    "",
    "```java",
    "company.updateDogs(hour, walked);",
    "return walked;",
    "```",
    "",
    "### Side Effect Pattern",
    "If a helper changes state, save any needed old value before the change.",
    "",
    "```java",
    "int available = company.numAvailableDogs(hour);",
    "int walked = Math.min(available, maxDogs);",
    "company.updateDogs(hour, walked);",
    "return walked;",
    "```",
    "",
    "### Common Traps",
    "- Calling an instance method on the class name.",
    "- Calling a method with no parameter when the header requires one.",
    "- Printing when the specification asks you to return.",
    "- Calling a mutating method twice inside one calculation.",
    "",
    "### Practice",
    "A class has an instance variable `tracker`. The method `tracker.getCount(day)` returns an int. The method `tracker.record(day, amount)` is void and changes the tracker. Write two lines that save the count for day 3, then record 5.",
    "",
    "<details class=\"answer-toggle\"><summary>Show worked answer</summary><div class=\"answer-body\">",
    "",
    "```java",
    "int count = tracker.getCount(3);",
    "tracker.record(3, 5);",
    "```",
    "",
    "</div></details>"
  ]),
  examLab("L-CLASS-FRQ-CHECKLIST", "Class Design FRQ Checklist", [
    "### Why This Lab Matters",
    "Class-design FRQs are friendly only if the structure is automatic. The algorithm may be interesting, but students often lose points before the algorithm begins.",
    "",
    "### Skeleton to Memorize",
    "Write the class, fields, constructor, and methods in separate places.",
    "",
    "```java",
    "public class Scoreboard {",
    "  private String team1;",
    "  private String team2;",
    "  private int score1;",
    "  private int score2;",
    "  private boolean team1Turn;",
    "",
    "  public Scoreboard(String t1, String t2) {",
    "    team1 = t1;",
    "    team2 = t2;",
    "    score1 = 0;",
    "    score2 = 0;",
    "    team1Turn = true;",
    "  }",
    "}",
    "```",
    "",
    "### Design Choices",
    "Instance variables should store everything that must persist after a method finishes. Local variables vanish when the method ends.",
    "",
    "Use a boolean for a two-state situation when possible.",
    "",
    "```java",
    "team1Turn = !team1Turn;",
    "```",
    "",
    "### Constructor Assignment",
    "If parameter names match field names, use `this` or choose different parameter names.",
    "",
    "```java",
    "public Player(String name) {",
    "  this.name = name;",
    "}",
    "```",
    "",
    "### Common Traps",
    "- `public class Name()` is not a valid class header.",
    "- A constructor has no return type.",
    "- Fields should be `private`.",
    "- Do not redeclare an instance variable as a local variable inside the constructor.",
    "- Return the specified value; do not replace the return with `System.out.println`.",
    "",
    "### Practice",
    "Design fields and a constructor for a `Timer` class that stores `minutes`, `seconds`, and whether the timer is running.",
    "",
    "<details class=\"answer-toggle\"><summary>Show worked answer</summary><div class=\"answer-body\">",
    "",
    "```java",
    "public class Timer {",
    "  private int minutes;",
    "  private int seconds;",
    "  private boolean running;",
    "",
    "  public Timer(int m, int s) {",
    "    minutes = m;",
    "    seconds = s;",
    "    running = false;",
    "  }",
    "}",
    "```",
    "",
    "</div></details>"
  ]),
  examLab("L-ARRAYLIST-OBJECTS", "ArrayList of Objects: Get, Construct, Add", [
    "### Why This Lab Matters",
    "Current ArrayList FRQs often store objects from a provided class. The task is not just `add`; it is get an object, call its methods, construct another object, and add that object to a result list.",
    "",
    "### Core Pattern",
    "An `ArrayList<Student>` stores `Student` references.",
    "",
    "```java",
    "Student s = roster.get(i);",
    "int score = s.getScore();",
    "```",
    "",
    "You can chain the call when it stays readable.",
    "",
    "```java",
    "int score = roster.get(i).getScore();",
    "```",
    "",
    "### Construct Then Add",
    "If the result list stores objects, add objects, not raw constructor arguments.",
    "",
    "```java",
    "ArrayList<Match> matches = new ArrayList<Match>();",
    "matches.add(new Match(players.get(low), players.get(high)));",
    "```",
    "",
    "### Copy vs Alias",
    "This does not make a copy:",
    "",
    "```java",
    "ArrayList<Student> temp = roster;",
    "```",
    "",
    "Both names now refer to the same list. If you must preserve the original, build a new list and add the elements you want.",
    "",
    "```java",
    "ArrayList<Student> temp = new ArrayList<Student>();",
    "for (Student s : roster) {",
    "  temp.add(s);",
    "}",
    "```",
    "",
    "### Common Traps",
    "- `list[i]` is array syntax; use `list.get(i)`.",
    "- `result.add(a, b)` does not create an object with `a` and `b`.",
    "- `new Match(i, j)` is wrong if the constructor expects `Competitor` objects.",
    "- Removing from an alias also removes from the original list.",
    "",
    "### Practice",
    "Given `ArrayList<Player> players`, create an `ArrayList<Game>` containing one game between the first and last player.",
    "",
    "<details class=\"answer-toggle\"><summary>Show worked answer</summary><div class=\"answer-body\">",
    "",
    "```java",
    "ArrayList<Game> games = new ArrayList<Game>();",
    "games.add(new Game(players.get(0), players.get(players.size() - 1)));",
    "```",
    "",
    "</div></details>"
  ]),
  examLab("L-UNUSUAL-TRAVERSALS", "Adjacent Pairs and Unusual Traversals", [
    "### Why This Lab Matters",
    "Many exam algorithms are not full left-to-right traversals. They compare neighbors, pair the ends, skip one item, or stop at a boundary that depends on another index.",
    "",
    "### Adjacent Pair Pattern",
    "To compare each item with the previous item, start at index 1.",
    "",
    "```java",
    "for (int i = 1; i < list.size(); i++) {",
    "  String prev = list.get(i - 1);",
    "  String cur = list.get(i);",
    "}",
    "```",
    "",
    "To compare each item with the next item, stop before the last index.",
    "",
    "```java",
    "for (int i = 0; i < list.size() - 1; i++) {",
    "  String cur = list.get(i);",
    "  String next = list.get(i + 1);",
    "}",
    "```",
    "",
    "### Two-End Pairing Pattern",
    "Use one index from the left and one from the right.",
    "",
    "```java",
    "for (int left = 0; left < list.size() / 2; left++) {",
    "  int right = list.size() - 1 - left;",
    "  // pair list.get(left) with list.get(right)",
    "}",
    "```",
    "",
    "For odd sizes, decide whether the middle or first item is skipped before writing the loop.",
    "",
    "### Return-After-Search Pattern",
    "Return failure only after the whole search has failed.",
    "",
    "```java",
    "for (int i = 0; i < values.length; i++) {",
    "  if (values[i] == target) {",
    "    return true;",
    "  }",
    "}",
    "return false;",
    "```",
    "",
    "### Common Traps",
    "- Starting at 0 and then using `i - 1`.",
    "- Looping through the last index and then using `i + 1`.",
    "- Returning `false` inside the loop after the first failed comparison.",
    "- Computing the right index as `size - i` instead of `size - 1 - i`.",
    "",
    "### Practice",
    "Write a loop header for comparing every adjacent pair in an `ArrayList<Integer> nums` using `i` and `i + 1`.",
    "",
    "<details class=\"answer-toggle\"><summary>Show worked answer</summary><div class=\"answer-body\">",
    "",
    "```java",
    "for (int i = 0; i < nums.size() - 1; i++) {",
    "  int a = nums.get(i);",
    "  int b = nums.get(i + 1);",
    "}",
    "```",
    "",
    "</div></details>"
  ]),
  examLab("L-2D-BOUNDARY-SUBSET", "2D Boundaries and Subset Traversals", [
    "### Why This Lab Matters",
    "Recent 2D array FRQs ask students to move through a grid without using a normal full traversal. The hard part is usually the boundary condition, not the array access itself.",
    "",
    "### Last Row and Last Column",
    "Use the last valid index, not the length itself.",
    "",
    "```java",
    "row == grid.length - 1",
    "col == grid[row].length - 1",
    "```",
    "",
    "### Guard Before Access",
    "Check whether a neighbor exists before reading it.",
    "",
    "```java",
    "if (row + 1 < grid.length) {",
    "  int below = grid[row + 1][col];",
    "}",
    "if (col + 1 < grid[row].length) {",
    "  int right = grid[row][col + 1];",
    "}",
    "```",
    "",
    "### Subset Traversal",
    "If a method says search row `row` and later rows, start there.",
    "",
    "```java",
    "for (int r = row; r < grid.length; r++) {",
    "  for (int c = 0; c < grid[r].length; c++) {",
    "    // inspect grid[r][c]",
    "  }",
    "}",
    "```",
    "",
    "### Same Location vs Same Value",
    "Two cells can have the same value but still be different cells. To avoid pairing an element with itself, compare the location.",
    "",
    "```java",
    "if (r != row || c != col) {",
    "  // not the exact same cell",
    "}",
    "```",
    "",
    "### Common Traps",
    "- `grid.length` is the number of rows; `grid[r].length` is the number of columns in row `r`.",
    "- Out-of-bounds access does not return `null`; it throws an exception.",
    "- `r != row && c != col` excludes the entire row and column, not just one cell.",
    "- A loop condition says when to continue, not when to stop.",
    "",
    "### Practice",
    "Write the condition for a loop that should continue until both `row` and `col` are at the bottom-right corner.",
    "",
    "<details class=\"answer-toggle\"><summary>Show worked answer</summary><div class=\"answer-body\">",
    "",
    "```java",
    "while (row < grid.length - 1 || col < grid[row].length - 1) {",
    "  // move row or col toward bottom-right",
    "}",
    "```",
    "",
    "</div></details>"
  ]),
  examLab("L-RANDOM-SIMULATION", "Random Simulation with Math.random", [
    "### Why This Lab Matters",
    "`Math.random()` appears in both MC and FRQ settings. The tested skill is not just calling the method; it is transforming the double into the exact probability or integer range.",
    "",
    "### Core Range Pattern",
    "For an inclusive integer range from `min` to `max`:",
    "",
    "```java",
    "int value = (int)(Math.random() * (max - min + 1)) + min;",
    "```",
    "",
    "Examples:",
    "",
    "```java",
    "int die = (int)(Math.random() * 6) + 1;     // 1 through 6",
    "int digit = (int)(Math.random() * 10);      // 0 through 9",
    "int food = (int)(Math.random() * 41) + 10;  // 10 through 50",
    "```",
    "",
    "### Probability Pattern",
    "`Math.random()` returns a double in `[0.0, 1.0)`. Compare it to the desired probability.",
    "",
    "```java",
    "if (Math.random() < 0.05) {",
    "  // 5% chance",
    "} else {",
    "  // 95% chance",
    "}",
    "```",
    "",
    "### Fill a 2D Array",
    "Random generation is often embedded inside a traversal.",
    "",
    "```java",
    "for (int r = 0; r < grid.length; r++) {",
    "  for (int c = 0; c < grid[r].length; c++) {",
    "    grid[r][c] = (int)(Math.random() * 9) + 1;",
    "  }",
    "}",
    "```",
    "",
    "### Common Traps",
    "- `Math.random(6)` is not valid.",
    "- `(int)Math.random() * 6 + 1` always casts the random double to 0 before scaling.",
    "- Multiplying by `max` only works when the range starts at 0 and has `max` possible values.",
    "- A simulation method may change instance variables; count only the days or iterations that actually happen.",
    "",
    "### Practice",
    "Write code for a 30% event and an integer from 4 through 12 inclusive.",
    "",
    "<details class=\"answer-toggle\"><summary>Show worked answer</summary><div class=\"answer-body\">",
    "",
    "```java",
    "if (Math.random() < 0.30) {",
    "  // event happens",
    "}",
    "",
    "int value = (int)(Math.random() * 9) + 4;",
    "```",
    "",
    "</div></details>"
  ])
];

const learningPaths = [
  {
    id: "path-foundations",
    navTitle: "1. Foundations",
    title: "1. Foundations: Values, Expressions, and Decisions",
    description: "Start with variables, expressions, output, input, and the Boolean decisions that make programs respond to data.",
    stepIds: ["1.1", "1.2", "1.3", "1.4", "1.5", "1.6", "2.2", "2.3", "2.4"]
  },
  {
    id: "path-methods-objects",
    navTitle: "2. Methods & Objects",
    title: "2. Methods, Libraries, and Objects",
    description: "Move from using Java APIs to understanding reference variables and object method calls.",
    stepIds: ["1.7", "1.8", "1.9", "1.10", "1.11", "B-OBJECT-MODEL", "1.12", "1.13", "1.14", "1.15"]
  },
  {
    id: "path-control-algorithms",
    navTitle: "3. Control Flow",
    title: "3. Control Flow as Algorithms",
    description: "Build reliable selection, loop, string, nested-loop, and run-time reasoning habits.",
    stepIds: ["2.1", "2.5", "2.6", "2.7", "2.8", "2.9", "2.10", "2.11", "2.12"]
  },
  {
    id: "path-class-design",
    navTitle: "4. Classes",
    title: "4. Designing Classes",
    description: "Shift from using objects to writing classes with constructors, methods, scope, and reference behavior.",
    stepIds: ["B-CLASS-DESIGN-STORY", "3.1", "3.3", "3.4", "3.5", "3.8", "3.9", "3.6", "3.7", "3.2"]
  },
  {
    id: "path-array-data",
    navTitle: "5. Arrays",
    title: "5. Arrays and Data Sets",
    description: "Learn fixed-size collections, traversals, text-file data, and the common algorithms that appear in MCQ and FRQ tasks.",
    stepIds: ["4.1", "4.2", "4.3", "B-COLLECTION-REFERENCES", "4.4", "4.5", "4.6"]
  },
  {
    id: "path-arraylist-data",
    navTitle: "6. ArrayList",
    title: "6. ArrayList and Growing Data",
    description: "Understand why ArrayList exists, why wrappers and object references matter, and how insertion/removal changes traversal strategy.",
    stepIds: ["B-ARRAYLIST-NEED", "4.7", "B-ARRAYLIST-BOXING", "B-COLLECTION-REFERENCES", "4.8", "4.9", "4.10"]
  },
  {
    id: "path-2d-grid",
    navTitle: "7. 2D Arrays",
    title: "7. 2D Arrays and Grid Algorithms",
    description: "Extend one-dimensional traversal into row-column reasoning for tables, boards, and matrix-style FRQs.",
    stepIds: ["B-ARRAY-TO-2D", "4.11", "4.12", "4.13"]
  },
  {
    id: "path-search-sort-recursion",
    navTitle: "8. Search & Recursion",
    title: "8. Search, Sort, and Recursion",
    description: "Connect traversal algorithms to binary search, sorting, recursive thinking, and recursive search/sort.",
    stepIds: ["4.14", "4.15", "B-ITERATION-RECURSION", "4.16", "4.17"]
  },
  {
    id: "path-frq-integration",
    navTitle: "9. FRQ Integration",
    title: "9. FRQ Integration",
    description: "Practice reading specifications and choosing the right tools for method, class, ArrayList, and 2D-array FRQs.",
    stepIds: ["B-FRQ-READING", "2.9", "3.5", "3.6", "4.5", "4.10", "4.13", "4.16"]
  }
];

const examLabs = [
  {
    id: "lab-core-idioms",
    navTitle: "Core Idioms",
    title: "Core Exam Idioms",
    description: "Practice the tiny Java patterns that current AP questions hide inside larger algorithms.",
    stepIds: ["L-MODULO-PATTERNS", "L-STRING-SURGERY", "L-RANDOM-SIMULATION", "L-METHOD-SIDE-EFFECTS"]
  },
  {
    id: "lab-frq-structures",
    navTitle: "FRQ Structures",
    title: "FRQ Structure Patterns",
    description: "Train the structures that recent FRQs repeatedly test: class design, object lists, traversals, and grid boundaries.",
    stepIds: ["L-CLASS-FRQ-CHECKLIST", "L-ARRAYLIST-OBJECTS", "L-UNUSUAL-TRAVERSALS", "L-2D-BOUNDARY-SUBSET"]
  },
  {
    id: "lab-all-current",
    navTitle: "All Labs",
    title: "All Current Exam Pattern Labs",
    description: "A compact practice sequence built from recurring patterns in recent AP CSA released questions and reports.",
    stepIds: ["L-MODULO-PATTERNS", "L-STRING-SURGERY", "L-RANDOM-SIMULATION", "L-METHOD-SIDE-EFFECTS", "L-CLASS-FRQ-CHECKLIST", "L-ARRAYLIST-OBJECTS", "L-UNUSUAL-TRAVERSALS", "L-2D-BOUNDARY-SUBSET"]
  }
];

const reviewSets = [
  {
    id: "review-reference-types",
    navTitle: "Reference Types",
    title: "Reference Types and Aliasing",
    description: "Review the concepts that make objects, arrays, wrappers, and ArrayLists behave differently from primitive variables.",
    stepIds: ["B-OBJECT-MODEL", "1.12", "1.13", "1.14", "3.6", "4.3", "B-COLLECTION-REFERENCES", "4.7", "B-ARRAYLIST-BOXING", "4.8"]
  },
  {
    id: "review-indexing-bounds",
    navTitle: "Indexing & Bounds",
    title: "Indexing and Bounds",
    description: "Strengthen zero-based indexing across String, arrays, ArrayList, and 2D arrays.",
    stepIds: ["1.15", "4.3", "4.4", "4.8", "4.9", "B-ARRAY-TO-2D", "4.11", "4.12"]
  },
  {
    id: "review-traversal-algorithms",
    navTitle: "Traversal",
    title: "Traversal Algorithms",
    description: "Practice reading, counting, accumulating, filtering, and mutating across every AP-tested collection type.",
    stepIds: ["2.7", "2.8", "2.10", "2.11", "4.4", "4.5", "4.9", "4.10", "4.12", "4.13"]
  },
  {
    id: "review-frq-readiness",
    navTitle: "FRQ Readiness",
    title: "FRQ Readiness",
    description: "Focus on the writing skills most likely to determine free-response score quality.",
    stepIds: ["B-FRQ-READING", "1.9", "2.9", "3.4", "3.5", "3.8", "4.5", "4.10", "4.13"]
  }
];

function parseTopics(markdown, unitInfo) {
  const headingPattern = /^## (\d+\.\d+) (.+)$/gm;
  const matches = Array.from(markdown.matchAll(headingPattern));

  return matches.map((match, index) => {
    const next = matches[index + 1];
    const start = match.index + match[0].length;
    const end = next ? next.index : markdown.length;
    const body = markdown.slice(start, end).trim();

    return {
      id: match[1],
      title: match[2].trim(),
      unit: unitInfo.unit,
      source: unitInfo.file,
      body
    };
  });
}

const units = unitFiles.map((unitInfo) => {
  const filePath = path.join(root, unitInfo.file);
  const markdown = fs.readFileSync(filePath, "utf8");

  return {
    unit: unitInfo.unit,
    title: unitInfo.title,
    file: unitInfo.file,
    topics: parseTopics(markdown, unitInfo)
  };
});

const payload = {
  title: "AP CSA Detailed Tutorials",
  topicCount: units.reduce((total, unit) => total + unit.topics.length, 0),
  bridgeLessons,
  examLabLessons,
  learningPaths,
  examLabs,
  reviewSets,
  units
};

fs.writeFileSync(
  outputFile,
  `window.APCSA_TUTORIAL_DATA = ${JSON.stringify(payload, null, 2)};\n`
);

console.log(`Wrote ${payload.topicCount} topics to ${path.relative(process.cwd(), outputFile)}`);
